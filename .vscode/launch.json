{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go", // Path to your main Go file
            "cwd": "${workspaceFolder}", // Set working directory
            "hideSystemGoroutines": true // Optional: Hide system goroutines
        }
    ]
}
